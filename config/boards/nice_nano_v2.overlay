&spi1 {
    compatible = "nordic,nrf-spim";
    status = "okay";
    mosi-pin = <6>;
    sck-pin = <5>;
    miso-pin = <7>;
};

/* This creates the label directly on the node reference */
nice_view_spi: &spi1 {};
